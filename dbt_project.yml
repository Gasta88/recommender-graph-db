name: "recommender_graph_db"

config-version: 2
version: "0.1"

profile: "recommender_graph_db"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"
  - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

models:
  recommender_graph_db:
    # materialized: table
